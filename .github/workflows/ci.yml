name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable
        with:
          components: rustfmt, clippy

      - name: Install native solver dependencies
        run: sudo apt-get update && sudo apt-get install -y clang libclang-dev libz3-dev

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run cargo check
        run: cargo check --all-targets --all-features --locked

      - name: Run clippy
        run: cargo clippy --all-targets --all-features --locked -- -D warnings

      - name: Run unit tests
        run: cargo test --lib --all-features --locked

      - name: Run integration tests
        run: cargo test --test main --all-features --locked

      - name: Run property tests
        run: cargo test property_tests --all-features --locked

      - name: Run determinism tests
        run: cargo test determinism_tests --all-features --locked

  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features:
          - ""
          - "lsp"
          - "dynamic-ffi"

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable

      - name: Install native solver dependencies
        run: sudo apt-get update && sudo apt-get install -y clang libclang-dev libz3-dev

      - name: Build with features
        run: |
          if [ -z "${{ matrix.features }}" ]; then
            cargo build --release --locked
          else
            cargo build --release --locked --features "${{ matrix.features }}"
          fi

  coverage:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable

      - name: Install native solver dependencies
        run: sudo apt-get update && sudo apt-get install -y clang libclang-dev libz3-dev

      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin

      - name: Generate coverage report
        run: cargo tarpaulin --all-features --out xml

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./cobertura.xml
          fail_ci_if_error: false

  security:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run security audit
        run: cargo audit

  msrv:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.85 (MSRV)
        uses: dtolnay/rust-action@1.85.0

      - name: Install native solver dependencies
        run: sudo apt-get update && sudo apt-get install -y clang libclang-dev libz3-dev

      - name: Check MSRV compatibility
        run: cargo check --all-targets --all-features --locked
