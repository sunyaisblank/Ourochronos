# Identity qubit channel. Every density operator is fixed, so computational
# basis-1 acceptance ranges from 0 to 1 and violates the decision promise.

QCHANNEL identity_qubit {
    QUBIT;
    KRAUS { C 1/1 0/1 C 0/1 0/1 C 0/1 0/1 C 1/1 0/1 };
    ACCEPT_BASIS 1;
}
